# Logitech Flow Overlay 验收清单

发布或交付新构建前必须完成：

验收结论分为三类，必须分别记录：

- **自动化验证通过**：仅表示测试和静态检查通过。
- **应用包构建通过**：仅表示正式名称与 Bundle ID 的产物可构建、签名和检查。
- **用户视觉验收通过**：必须由用户在实际显示器和系统设置下亲自确认。

前两类结论不得写成“效果验收通过”。没有用户明确确认时，视觉验收状态始终为
“待用户验收”。

1. `swift test` 覆盖左、右、上、下外侧边缘以及多显示器内部接缝。
2. 构建 `.app` 后验证签名、Info.plist 和可执行文件。
3. 启动构建产物，确认主界面自动显示并满足：
   - Dock 与菜单栏都有应用入口；
   - 状态卡片显示当前鼠标或 Options+ 状态；
   - “蒙层设置”可以分别修改透明度、毛玻璃强度和文字，退出并重启应用后
     设置仍然保留；
   - “鼠标连回位置提醒”默认启用，可以单独关闭，并可修改界面变暗程度、
     提醒持续时间、聚光圆形半径和边缘羽化程度；退出并重启应用后设置仍然
     保留；
   - 新安装的连回提醒默认值固定为变暗程度 50%、持续时间 2.0 秒、
     聚光半径 100 pt、边缘羽化 4%；
   - 四个参数标签固定为“变暗程度”“持续时间”“聚光半径”“边缘羽化”，
     中文标签不得超过四个字；
   - 按下任意连回提醒滑块时，真实多显示器聚光层必须立即显示且不拦截
     滑块操作；聚光圆形跟随滑块上的指针，让用户直接看到变暗、半径和羽化
     的实时变化，松开后预览立即消失；
   - 新安装默认值必须为蒙层透明度 20%、毛玻璃强度 80%；
   - 透明度范围为 10%–85%，只改变深色蒙层且数值越小时背景越透明；
   - 毛玻璃必须使用 `NSVisualEffectView` 的 `.behindWindow` 与
     `.fullScreenUI`；毛玻璃强度只能通过该材质的 `maskImage` alpha 控制，
     不得使用视图透明度、灰色蒙层透明度或无跨窗口输入的
     `backgroundFilters` 模拟高斯模糊半径；
   - 毛玻璃强度范围固定为 0%–100%，并将系统材质有效的 60%–100% alpha
     非线性映射到完整滑块：界面 0% 对应材质 60%，界面 50% 对应约 92%，
     界面 100% 对应材质 100%；全程必须产生清晰、连续的视觉变化，调整
     强度不得改变深色蒙层；
   - “两个独立滑块、透明度 10%–85%、毛玻璃强度 0%–100% 且全程视觉
     有效”是固定产品
     契约；没有用户明确要求时不得删除滑块、合并控制或修改范围；
   - 拖动任意滑块时真实全屏遮罩显示在主窗口后方，松开后预览消失；
   - 关闭窗口不会退出应用，通过 Dock 或菜单栏可以重新打开。
4. 分别以中文和非中文首选语言启动应用，确认主界面、菜单和遮罩分别显示
   中文与英文。
   - 用户界面统一使用“当前活动设备检测”/“Active Device Detection”；
   - “边缘检测”只用于解释内部判断方式，不得作为菜单或设置项名称。
5. 确认系统临时目录中的 `LogitechFlowOverlay.log` 同时记录：
   - `optionsPlusRunning=true`
   - `hidPointers`（仅用于诊断，不得作为 Flow 检测前置条件）
6. 使用菜单“预览遮罩”确认所有显示器出现遮罩并在 3 秒后消失。
   - 自定义文字在每个显示器上完整显示，长文字最多换行 3 行；
   - 分别预览两个滑块的最低和最高值，确认深色蒙层与系统毛玻璃彼此独立；
   - 图标和提示文字位于中央圆角卡片内；
   - 每个显示器在卡片下方显示独立的圆形 `X` 按钮，按钮不得显示文字，
     `X` 必须使用与提示内容一致的亮白色，点击后立即关闭本次遮罩；
   - 日志中的 `overlayShown count` 必须等于 `NSScreen.screens.count`。
   - 每个遮罩必须有不同的 `window` 与 `display`，且 `target` 等于 `actual`、`visible=true`。
   - 使用 Quartz 窗口列表确认屏幕上的 Logitech Flow Overlay 遮罩窗口数等于显示器数；仅验证数组数量不算通过。
7. 以最终用户方式双击 DMG，确认 Finder 自动显示图标视图：
   - 应用和 Applications 快捷方式分别位于箭头两侧；
   - 拖拽说明位于箭头附近；
   - 窗口以中英文直接显示“系统设置 → 隐私与安全性 → 仍要打开”；
   - 映像中没有额外安装说明文档。
8. 使用“预览提醒”确认：
   - 每块显示器均被独立的半透明窗口覆盖，窗口不拦截鼠标点击；
   - 只有鼠标所在显示器显示带柔和边缘的圆形亮区；
   - 移动鼠标时亮区持续跟随，跨显示器后亮区切换到对应显示器；
   - 到达设定持续时间后所有变暗窗口自动淡出；
   - 调整变暗程度、聚光半径和边缘羽化后，预览与设置一致；
   - 羽化 0% 显示清晰硬边，羽化值增大时过渡带连续变宽、变柔和。
9. 真实 Flow 往返一次；日志必须出现 `flowCandidate`、`flowAway`、
   `flowReturned`、`reconnectAlertShown` 和 `reconnectAlertHidden`。
   - 返回时离开遮罩消失，鼠标连回位置提醒同时出现；
   - 手动点击离开遮罩的 `X` 不得触发鼠标连回位置提醒；
   - 关闭“鼠标连回位置提醒”后，真实 Flow 返回不得显示变暗窗口。
   - 鼠标处于另一台设备时，即使日志出现 `optionsPlusProbeMissing` 或稳定的
     `optionsPlusRunning=false`，已经确认的离开状态和遮罩仍然保留；
   - Options+ 恢复后，日志出现 `flowAwayPreserved`，鼠标返回时仍必须出现
     `flowReturned` 和 `reconnectAlertShown`。

在第 9 项没有真实设备操作证据时，只能声明自动化和启动验证通过，不能声称真实 Flow 已验证。
